services:
  # ── Backend (FastAPI API) ──
  - type: web
    name: marketmind-api
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: gunicorn -w 4 -k uvicorn.workers.UvicornWorker --chdir backend app.main:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: marketmind-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: marketmind-redis
          property: connectionString
      - key: PYTHON_VERSION
        value: 3.11.5
      - key: CORS_ORIGINS
        value: "https://marketmind-terminal.vercel.app,http://localhost:8080,http://localhost:5173"

  # ── Celery Worker (Background Process) ──
  - type: worker
    name: marketmind-worker
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: celery -A app.workers.celery_app.celery --workdir backend worker --loglevel=info -P solo
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: marketmind-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: marketmind-redis
          property: connectionString
      - key: PYTHON_VERSION
        value: 3.11.5

  # ── Redis (Cache/Queue) ──
  - type: redis
    name: marketmind-redis
    plan: free
    ipAllowList: [] # Allow all internal connections

databases:
  - name: marketmind-db
    plan: free
